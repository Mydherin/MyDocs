# Guidelines

- Use hexagonal architecture
- Use clear, modular, decoupled and cohesionate folder structure
- Use SRP principle in classes keeping the modular, decoupled and cohesionate
- Use lombok to avoid boilerplate
- Use lambdas
- Allow all origins in CORS